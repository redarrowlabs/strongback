/// <reference types="react" />
export interface InstantDateViewProps {
    date: string;
    pattern?: string;
}
/**
 * Displays the date of an instant, relative to the viewer.
 */
export declare function InstantDateView(props: InstantDateViewProps): JSX.Element;
